#include <stdio.h>#include <stdlib.h>struct clientData{	int acctNum; //账户账号	char lastName[15]; //户主名	char firstName[10];//户主姓	float balance;  //账户余额};int main(){	int i;	struct clientData blankClient = { 0,"","",0.0 };	FILE * cfPtr;	if ((cfPtr = fopen("C:\\client.dat", "wb")) == NULL) /*打开文件*/		printf("File could not be opened\n");	else {		for (i = 1; i <= 100; i++)			fwrite(&blankClient, sizeof(struct clientData), 1, cfPtr);		fclose(cfPtr);	}	return 0;}